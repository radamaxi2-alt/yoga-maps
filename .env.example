# Supabase public project settings. Safe to expose to the browser when RLS is correct.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Google Maps browser key. Restrict it by domain and enabled APIs in Google Cloud.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
